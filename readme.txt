React is a javascript library for building fast and interactive user interfaces.
A component is a piece of the UI. components are build independent, isolated and reusable components.

React reacts to state changes and updates the DOM.

An expression is something which produces a value.